# Snippet

Snippet is a browser-based video editing program written in Svelte and TypeScript.

## Developing

To run the development server:

```sh
pnpm install
pnpm run dev
```
To build a production version:

```sh
pnpm run build
```
